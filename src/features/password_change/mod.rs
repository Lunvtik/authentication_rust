//! Slice: PasswordChange (Passwort ändern, nur für eingeloggte Nutzer).
//! Scharf schalten auf dem password-change-Branch: Submodule einkommentieren und in
//! src/features/mod.rs `pub mod password_change;` ergänzen.
//! Schritte: docs/checkliste_a_kern.md -> PasswordChange.

// pub mod handler;   // HTTP: Formular, POST verarbeiten
// pub mod service;   // Logik: altes Passwort prüfen, neues hashen, DB aktualisieren
// pub mod error;     // PasswordChangeError
